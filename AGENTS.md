# AGENTS.md — SecondBrain Lite 协作规范

## 1. 项目目标与当前状态

SecondBrain Lite 是面向个人用户的本地 Markdown 知识库。笔记源数据始终是普通 UTF-8 `.md` 文件；应用提供浏览器编辑、预览、wikilink/反向链接、文件树、标签、仪表盘和力导向图谱。

v2 的界面链接和手动标签存储在笔记库的 `.sb/links.json`，不得为了界面关系静默改写笔记正文。

截至 2026-08-20 的已验证基线：

- 依赖已安装，`tsc --noEmit` 通过，已有 `dist/` 前端产物。
- 已有基于 Node Test Runner 的服务端安全、保存、可恢复文件管理、文件导入、统一知识条目、附件预览、Markdown 渲染安全和本地全文搜索回归测试（当前 64 项）；已有中英文 README、`.gitignore`、`main` 分支和 GitHub Actions 多版本检查，但仍没有 lint、安装包或正式独立服务端产物。
- 文档把 MVP 和 v2 标记为完成，但真实代码仍有未闭环功能和安全问题；不能仅凭进度勾选判断完成。
- 2026-08-20 补充：普通 `.drawio` 源文件现在按独立 Draw.io 类型提供不超过 1 MB 的安全只读 XML 结构预览；它仍不会进入正文搜索、不会执行 XML，也不会提供编辑能力。Office 文件仍仅管理。
- 2026-08-20 补充：文件树提供按 Markdown、Draw.io、图片、PDF、音视频及文档/其他附件筛选的本地视图；筛选只影响显示，不改变磁盘数据或目录结构。
- 2026-08-20 补充：统一知识切换器现在显示浏览器本地保存的收藏和最近访问路径；这些偏好不写入笔记库，删除或移动文件后旧路径可能需要用户手动清理。
- 2026-08-20 补充：概览页健康摘要现在统计失效 wikilink、孤立条目和未加手动标签附件；这是只读诊断，不会自动改写用户数据。
- 2026-08-20 补充：健康摘要可展开最多 5 条失效链接和未加标签附件，并直接打开对应条目；超出部分只显示数量，不执行自动修复。
- 2026-08-20 补充：失效链接详情支持打开来源文件或复制 `[[目标]]` 文本；系统不自动改写笔记。
- 2026-08-20 补充：失效 wikilink 可通过概览页搜索 Markdown 目标并进行单条替换；替换先读取版本并使用条件保存，冲突时拒绝覆盖。
- 2026-08-20 补充：重复标题详情会建议改为唯一名称或使用界面链接；当前不支持路径型 wikilink，不应在文档中暗示该能力。
- 服务监听范围、文件/图片路径逃逸、笔记保存冲突、回收站恢复、附件预览、统一知识条目、Markdown 渲染安全、Wikilink 精确跳转、LinkStore 损坏/外部改动保护及编辑后的索引一致性已建立回归测试；图片、PDF、`.drawio.svg`、`.drawio.png` 和不超过 1 MB 的 UTF-8 文本/代码文件可只读预览，音视频可通过浏览器原生控件本地播放，普通 `.drawio` 和 Office 文件仍只能管理。所有可见普通文件现在都可作为知识条目进入统一搜索、手动标签、界面链接、反向链接、全局/局部图谱；附件图谱节点按文件类型区分颜色，点击会打开对应预览、播放或仅管理页，但不会解析附件正文或生成自动语义关系。当前普通内容保存使用局部索引刷新，标题变更和结构性变化仍回退到全量重建；编辑器、图谱、Markdown 预览等重前端模块已按需加载，全局图谱超过 1000 节点时会先提示再渲染，标签视图已区分加载、空数据和错误状态，链接面板可回读手动标签并显示加载/搜索/保存失败状态，反向链接面板已区分反链/未链接提及的加载、空数据和失败状态，文件树“链接到...”入口已补充搜索/添加链接状态反馈，文件树本体已补充标题索引降级提示、手动刷新、预览/播放/仅管理标识、产品内新建弹窗和产品内重命名/移动弹窗，移入回收站、未保存切换、重新加载和覆盖保存已使用产品内确认弹窗，wikilink 解析失败和歧义已使用产品内提示，前端已清空原生 `alert/prompt/confirm`，非 Markdown 附件已有预览、播放或仅管理主区域说明；附件页面已补充文件名、位置、扩展名、类型、文件大小、修改时间、能力说明和知识关系侧栏，且详情渲染已收敛到共享 `FileDetails` 与 `useFileMetadata`，并支持复制相对路径、复制 Markdown 引用，以及在保留后台当前笔记的前提下将附件引用插入到当前笔记末尾；编辑器现已提供附件搜索选择器，可筛选非 Markdown 文件并将引用插入当前光标，本地图片引用会通过受保护的预览接口渲染，Markdown 预览中的本地非 Markdown 附件链接可直接打开应用内预览、播放或仅管理页面；仅管理页额外包含 `.drawio` 导出预览引导；附件元数据来自只读 `/api/files/meta`，并已覆盖路径边界回归测试。QuickSwitcher 已升级为统一知识切换器，可按标题、文件名或路径搜索笔记和附件，显示文件类型并通过键盘或鼠标打开正确页面；wikilink 自动补全仍只搜索 Markdown。今日笔记按钮已有创建中和失败提示，常见内联提示已开始收敛到共享 `InlineNotice` 组件，大型知识库的真实交互渲染体验仍需继续验证。
- 概览页已统一统计所有可见知识条目：分别显示 Markdown 与附件数量，使用合并去重后的 wikilink/界面链接计算关系，核心条目、孤立条目、目录分组和最近修改均覆盖附件并支持打开对应页面；根目录不再伪装为 `.` 分组。
- 已新增独立全文搜索视图和 `Ctrl/Cmd + Shift + F` 快捷键：所有可见知识条目均可按标题或路径检索，不超过 1 MB 的 UTF-8 Markdown、文本和代码文件还可按正文检索并显示安全的纯文本命中片段；搜索仅在本机内存中完成，PDF、图片、Office、音视频和普通 `.drawio` 不会被解析正文。
- 新生成的附件引用和剪贴板图片引用使用相对于当前笔记的显式 `./` 或 `../` 路径，便于在其他 Markdown 编辑器中继续使用；预览器按当前笔记解析这些显式路径，同时保留对历史无前缀“笔记库根路径”引用的兼容。
- 应用所有主视图均可接收本机文件或文件夹拖放，并在松手前明确显示复制目标：编辑页使用当前条目所在目录，其他视图使用知识库根目录；文件树还提供“文件”和“文件夹”选择入口。文件夹会递归复制并保留层级，安全合并已有真实目录但绝不覆盖同名文件，隐藏条目和 `node_modules` 会跳过；拖拽目录可保留空目录，受浏览器目录选择器能力限制，选择文件夹时只能获得包含文件的目录。单文件限制 256 MiB，流式写入，批量导入允许部分成功并显示失败原因。成功导入的非 Markdown 文件可一键插入当前笔记，并会成为可手动组织的图谱节点；外部原文件同步、附件正文抽取和自动语义关联尚未实现。

## 2. 技术栈

- Node.js >= 18、npm、TypeScript（strict）
- 前端：React 18、Vite 5、Tailwind CSS 3
- 编辑器/渲染：CodeMirror 6、`marked`、`gray-matter`
- 图谱：D3.js 7
- 后端：Express 4、REST、`ws`
- 文件监听：`chokidar`
- 输入校验：Zod 3
- CLI/开发：`commander`、`tsx`、`concurrently`

以 `package.json` 和 `package-lock.json` 的实际声明为准，不凭文档猜测版本或依赖。

## 3. 目录说明

```text
src/
├── cli.ts                  # sb start CLI
├── shared/                 # 前后端共享常量、文件类型和回收站契约
├── server/
│   ├── index.ts            # Express + HTTP/WS 启动入口
│   ├── routes/             # 文件、笔记、链接、标签 API
│   └── services/           # 索引、监听、文件管理、图片、LinkStore
└── client/
    ├── App.tsx             # 视图切换和应用级状态
    ├── components/         # 编辑器、文件树、图谱及知识视图
    ├── hooks/              # 图片粘贴、WebSocket
    ├── lib/api.ts          # 前端 API 客户端
    └── types/              # 前端数据类型
bin/sb.cjs                  # Windows 兼容 CLI 包装器
memory-bank/                # PRD、架构、技术栈、计划和进度
docs/superpowers/           # v2 设计与实施计划
.superpowers/sdd/           # 历史任务报告，不是测试证据
.claude/                    # Claude Code 本地配置
dist/                       # Vite 生成产物，不手工编辑
```

## 4. 启动、测试、构建和打包

本机 PowerShell 的 `npm.ps1` 可能被执行策略拦截，优先使用 `npm.cmd`。

```powershell
# 开发：后端默认 3000，Vite 默认 5173
$env:SB_NOTES_DIR='D:\path\to\vault'
npm.cmd run dev

# 分别启动
npm.cmd run dev:server
npm.cmd run dev:client

# 使用已有前端产物启动；务必明确传入笔记库
npm.cmd start -- D:\path\to\vault --no-open
# 等价入口
node bin\sb.cjs start D:\path\to\vault --no-open

# 当前类型检查
npm.cmd run typecheck

# 服务端安全与保存回归测试
npm.cmd test

# 前端生产构建（先类型检查）；会改写 dist/
npm.cmd run build
```

- 当前没有 `lint` 或 `package` 命令，不得报告为已通过。
- `npm run build` 只构建前端；服务端仍由 `tsx` 运行 TypeScript，不等于完成正式打包。
- 启动服务会创建或更新笔记库的 `.sb/links.json`。测试时只使用临时笔记库，不得把项目根目录或用户真实笔记库当测试数据。
- 不手工修改 `dist/`；仅在任务要求并允许生成产物时运行构建。

## 5. 编码规范

- 修改前先完整阅读 `memory-bank/*.md`、本文件、`CLAUDE.md` 及与任务直接相关的 spec/plan。
- 只改完成当前任务所需的代码；不顺手重构、格式化或清理无关代码。
- 保持 TypeScript strict；禁止新增 `any`。确有第三方类型边界时，使用最窄的局部适配并说明原因。
- 新文件不超过 300 行。文件预览/管理状态已从 `src/client/App.tsx` 拆分到 `useWorkspaceFileViews`，当前相关核心文件均低于 300 行；后续让文件接近限制时，应优先按当前需求拆分职责。
- 后端所有外部输入必须经过 Zod 和业务语义校验；`z.string()` 不等于路径、文件或链接目标安全。
- 文件系统操作使用异步 API；禁止在循环中执行同步文件 I/O。
- 前端 API 调用统一放在 `src/client/lib/api.ts`，数据加载逻辑优先封装为职责明确的 hook。当前没有文档曾要求的 `useNotes`，不得假装其存在。
- 不新增前后端跨层 import。当前 `link-store.ts` 从 `client/types` 做 type-only import 是已知遗留冲突。
- 端口应来自 CLI、环境或集中配置。当前 Vite proxy 和共享常量仍有硬编码，是已知遗留冲突。
- 不新增临时 `console.log`；现有生产日志属于待治理项，不在无关任务中批量删除。

## 6. UI 和产品约束

- 保持“本地优先、Markdown 笔记为核心、用户拥有数据”的核心价值；图片、PDF 和受大小限制的文本/代码仅提供只读预览，音视频仅使用浏览器原生控件本地播放，Office 和普通 `.drawio` 等文件不得伪装成已支持编辑或预览。
- 导入意味着把外部文件复制进当前笔记库，不得暗示为外部文件链接或双向同步；只有 Markdown 笔记具备正文编辑、wikilink 解析和未链接提及扫描，其他格式可使用手动标签、界面链接、反向链接和图谱节点，但必须按其实际预览、播放或仅管理能力展示。
- UI 以现有中文、浅色、单窗口布局为基线；除非需求明确，不新增主题、插件、云同步、认证或移动端架构。
- `[[]]` 内联链接与 `.sb/links.json` 界面链接必须共存，不能破坏已有笔记。
- 文件夹路径是隐式标签，手动标签是独立数据；二者在 UI 中要能明确区分。
- 不把“组件存在”当作“功能完成”。涉及图片预览、wikilink 跳转、标签回读、每日笔记、局部图谱时，应先核对当前已知缺口。
- 导航或外部文件更新不得静默覆盖未保存内容；若任务触及该流程，必须明确冲突处理策略。

## 7. 数据及安全约束

- 产品宣称仅本地运行，因此 HTTP/WS 必须持续限制在 loopback，不得回退为局域网监听。
- 所有读、写、重命名、删除、上传路径必须继续使用统一真实路径边界检查，拒绝绝对路径、`..`、同前缀兄弟目录和符号链接逃逸。
- 通用文件导入必须保持流式大小限制、隐藏路径保护和无覆盖发布；上传失败或超限后不得遗留可见半成品文件。
- 未经用户明确要求，不递归删除目录，不覆盖已有笔记，不操作工作区外文件。
- `.sb/links.json` 是用户数据。读取失败、格式损坏或未知版本时必须保留原文件并阻止覆盖；有效修改前写入 `links.json.bak`，再原子替换主文件。运行期间检测到外部修改时返回冲突，要求重新加载后再写。
- Markdown/HTML 进入 `dangerouslySetInnerHTML` 前必须清洗；wikilink 标题、别名和 URL 必须转义/限制协议。当前原始 HTML 会按文本转义，禁止在没有等效安全测试的情况下重新开启。
- 不向外网发送笔记、路径、标签或图谱数据。任何联网、遥测、云服务或新依赖安装都必须先获得用户许可。
- 安全和文件系统测试只使用明确创建的临时目录，并在删除前验证其绝对路径和范围。

## 8. 修改前后的验证要求

修改前：

1. 写出任务假设、范围和可验证成功标准。
2. 阅读相关实现、调用方、文档和历史任务报告；报告只作线索，不作通过证据。
3. 检查 Git 状态；当前仓库使用 `main` 分支，必须保护已有提交和未提交改动。
4. 优先写能复现缺陷的测试。若现有测试基础不足，说明将使用的最小验证方式，不凭空宣称 E2E。

修改后至少执行：

1. `tsc --noEmit`。
2. `npm.cmd test` 及与改动直接相关的自动化测试；现有套件未覆盖时，使用临时笔记库做可重复的定向验证并记录步骤。
3. 修改前端、Vite 或依赖边界时运行 `npm.cmd run build`；注意这会更新 `dist/`。
4. API/文件系统改动必须覆盖合法路径和拒绝路径；持久化改动必须覆盖损坏数据和失败恢复。
5. 检查完整 diff，确保每一处改动都能追溯到任务，并确认没有修改禁止范围。

不得仅以“TypeScript 零错误”证明功能、性能、安全或端到端流程完成。

## 9. 默认禁止修改的范围

除非任务明确要求并获得相应授权，不修改：

- `CLAUDE.md`（保留为 Claude Code 兼容规则）
- `.claude/settings.local.json`
- `node_modules/`、`dist/`、`package-lock.json`
- `memory-bank/`、`docs/`、`.superpowers/sdd/` 中的历史记录
- 用户真实笔记库及其 `.sb/` 数据
- 工作区外文件

同时禁止：擅自安装/升级依赖、访问网络、初始化 Git、批量格式化、删除历史文件、执行 `git reset --hard`、`git clean` 或等效破坏性操作。

## 10. 当前状态的权威来源

发生冲突时按以下方式判断，而不是静默选边：

1. `package.json`、配置和实际源码：当前“实现了什么”的最高证据。
2. `AGENTS.md`：后续协作和变更流程的执行规则。
3. `memory-bank/product-requirements-document.md`：产品目标；当前状态仍为草稿。
4. `memory-bank/architecture.md`、`memory-bank/progress.md`：项目维护者记录的架构/进度，但部分内容已过时，必须与代码核对。
5. `docs/superpowers/specs/2026-07-17-sb-v2-no-touch-links-design.md`：v2 设计意图；状态仍为“待评审”，部分功能未实现。
6. `CLAUDE.md`：遗留 AI 规则；继续保留。与本文件或代码冲突时必须指出，不得自行猜测。
7. `.superpowers/sdd/*-report.md`：历史执行报告，仅能证明当时声明，不能替代测试或 Git 历史。

## 11. Git 提交和回滚

- 当前仓库已经初始化并使用 `main` 分支；不要擅自改写远程、分支或提交历史。
- 只有用户明确要求时才创建提交。若以后存在 Git，修改前先查看 `git status` 和相关 diff，保护用户已有改动。
- 一个提交只包含一个可验证目标；提交信息说明行为变化，不使用“misc/fix stuff”等模糊描述。
- 提交前完成本文件要求的验证，不提交 `node_modules/`、临时笔记库、日志、缓存或未经要求的 `dist/`。
- 回滚优先使用新提交 `git revert` 或经确认的定向反向补丁；禁止用 `reset --hard`、`checkout --` 或 `clean` 覆盖用户工作。
- 若发现 Git 状态异常或远程历史不一致，任何高风险修改前都应先说明并等待用户确认。
